Атрибуты `dataContainer` и `property` используются для связывания `MarkdownEditor` с атрибутом сущности.

`dataContainer` содержит экземпляр сущности, `property` указывает атрибут сущности для редактирования.
