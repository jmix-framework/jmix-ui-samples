The `dataContainer` and `property` attributes are used to connect the `MarkdownEditor` to an entity attribute.

The `dataContainer` contains an entity instance; `property` specifies an attribute to edit.
